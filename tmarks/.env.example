# TMarks 环境变量配置示例
# 此文件仅用于参考，实际配置在 .env.production 和 wrangler.toml 中

# ============================================
# 前端环境变量（构建时使用）
# ============================================
# 这些变量会在构建时被打包到代码中
# 配置文件：.env.production

# API 基础 URL（默认为 /api/v1）
VITE_API_URL=/api/v1

# ============================================
# 后端环境变量（运行时使用）
# ============================================
# 这些变量在 Cloudflare Workers 运行时使用
# 配置文件：wrangler.toml

# 所有后端配置都在 wrangler.toml 中：
# - JWT_SECRET（建议使用 wrangler secret put）
# - ENCRYPTION_KEY（建议使用 wrangler secret put）
# - ENVIRONMENT
# - JWT_ACCESS_TOKEN_EXPIRES_IN
# - JWT_REFRESH_TOKEN_EXPIRES_IN
# - ALLOW_REGISTRATION
# - D1 数据库绑定
# - KV 命名空间绑定

# ============================================
# 部署流程
# ============================================
#
# 1. 构建前端（使用 .env.production）:
#    npm run build
#
# 2. 配置 Secrets（推荐，仅首次需要）:
#    wrangler secret put JWT_SECRET
#    wrangler secret put ENCRYPTION_KEY
#
# 3. 运行数据库迁移:
#    npm run db:migrate
#
# 4. 部署到 Cloudflare:
#    npm run cf:deploy
#
